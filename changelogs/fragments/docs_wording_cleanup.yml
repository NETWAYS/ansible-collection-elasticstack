trivial:
  - The repository requirement is worded the same way in all four roles that need it, and the
    requirements that stated the obvious are gone.
  - The pipelines documentation no longer labels the generated Redis input block as an output,
    and the prose around the examples says what the role actually does.
  - "The three ``beats_<beat>_output`` descriptions now say what choosing each value changes
    instead of repeating the variable name, the README no longer names the tested ansible-core
    versions, and the stale variable renaming notice is removed."
