# Logstash role

The Logstash role documentation now lives with the role itself:

**➜ [roles/logstash/README.md](../roles/logstash/README.md)**

It covers requirements, usage, the full variable reference (generated from
`roles/logstash/meta/argument_specs.yml`) and the
[pipelines documentation](../roles/logstash/docs/pipelines.md).
