---
- name: Prepare
  hosts: all
  tasks:
    - name: Install packages needed for Debian
      apt:
        name:
          - gpg
          - curl
          - procps
          - gpg-agent
          - openssl
          - git
          - openssl
        update_cache: yes
