---
roles:
  # Versions higher than 1.6.0 seem to have a problem with idempotency on rocky 9 python 3.9, NOT on ubuntu2204 python 3.10
  - name: geerlingguy.redis
    version: "1.6.0"

collections:
  - community.general
