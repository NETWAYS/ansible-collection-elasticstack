---
galaxy_info:
  role_name: logstash
  author: netways
  description: Logstash for Linux
  company: "Netways GmbH"
  license: license (GPL-3.0-or-later)
  min_ansible_version: "2.14"  # max supported: 2.20  # Minimum version specifications are only for versions we have not had problems with; we will raise them when larger issues arise.
  platforms:
    - name: EL
      versions:
        - "8"
    - name: Debian
      versions:
        - bullseye
    - name: Ubuntu
      versions:
        - focal
        - jammy
  galaxy_tags:
    - elk
    - logstash
    - logging
    - system

collections:
  - netways.elasticstack

dependencies:
  - geerlingguy.redis
