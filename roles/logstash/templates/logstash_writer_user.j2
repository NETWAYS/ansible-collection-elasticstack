{
{% if logstash_password_hash | bool %}
  "password_hash" : "{{ logstash_password | password_hash(logstash_password_hash_algorithm , lookup('password', '/dev/null', chars=['ascii_letters', 'digits'], length=logstash_password_salt_length)) }}",
{% else %}
  "password" : "{{ logstash_password }}",
{% endif %}
  "roles" : [ "logstash_writer"],
  "full_name" : "Internal Logstash User"
}
