{
{% if logstash_password_hash | bool %}
  "password_hash" : "{{ logstash_password | password_hash('sha256', lookup('password', '/dev/null chars=ascii_letters,digits length=16')) }}",
{% else %}
  "password" : "{{ logstash_password }}",
{% endif %}
  "roles" : [ "logstash_writer"],
  "full_name" : "Internal Logstash User"
}
