{{ ansible_managed | comment }}

input {
  redis {
    host => "localhost"
    data_type => "list"
    key => "forwarder"
{% if logstash_redis_password is defined %}
    password => "{{ logstash_redis_password }}"
{% endif %}
  }
}
